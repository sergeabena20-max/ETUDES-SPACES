import Link from "next/link";
import { prisma } from "@/lib/prisma";

export const dynamic = "force-dynamic";

export default async function CoursesPage(){const courses=await prisma.course.findMany({where:{status:"PUBLISHED"},include:{subject:true,academicLevel:true},orderBy:{createdAt:"desc"},take:24});return <main className="min-h-screen"><header className="border-b bg-white"><div className="container flex items-center justify-between py-4"><Link href="/" className="font-black">Études <span className="gradient-text">Space</span> 🇨🇲</Link><Link href="/dashboard" className="text-sm font-semibold text-sky-600">Mon espace</Link></div></header><section className="container py-10"><p className="text-sm font-semibold text-sky-600">📚 Ressources</p><h1 className="mt-2 text-4xl font-black">Cours</h1><p className="mt-2 text-slate-500">Découvre les cours disponibles sur Études Space.</p><div className="mt-8 grid gap-4 md:grid-cols-2 lg:grid-cols-3">{courses.length?courses.map(c=><article key={c.id} className="card p-5"><div className="text-xs font-bold uppercase text-sky-600">{c.subject?.name||"Matière"} · {c.academicLevel?.name||"Tous niveaux"}</div><h2 className="mt-3 text-xl font-bold">{c.title}</h2><p className="mt-2 text-sm leading-6 text-slate-500">{c.description||"Cours disponible."}</p></article>):<div className="card p-8 md:col-span-3"><h2 className="font-bold">Les premiers cours arrivent bientôt.</h2><p className="mt-2 text-sm text-slate-500">Le catalogue sera alimenté depuis l'administration.</p></div>}</div></section></main>}
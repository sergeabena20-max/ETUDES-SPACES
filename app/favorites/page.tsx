import Link from "next/link"; import {redirect} from "next/navigation"; import {getCurrentUser} from "@/lib/session"; import {prisma} from "@/lib/prisma";
export const dynamic="force-dynamic";
export default async function Favorites(){const u=await getCurrentUser();if(!u)redirect("/login");const fav=await prisma.favorite.findMany({where:{userId:u.id},include:{course:true,exam:true},orderBy:{createdAt:"desc"}});return <main className="min-h-screen"><header className="border-b bg-white"><div className="container flex items-center justify-between py-4"><Link href="/dashboard" className="font-black">Études <span className="gradient-text">Space</span> 🇨🇲</Link><Link href="/dashboard" className="text-sm text-sky-600">Mon espace</Link></div></header><section className="container py-10"><h1 className="text-4xl font-black">Mes favoris ❤️</h1><div className="mt-8 grid gap-4 md:grid-cols-2">{fav.length?fav.map(f=><article key={f.id} className="card p-5"><div className="text-xs font-bold text-sky-600">{f.course?"📚 Cours":"📝 Épreuve"}</div><h2 className="mt-2 font-bold">{f.course?.title||f.exam?.title}</h2></article>):<div className="card p-7 md:col-span-2"><p className="font-bold">Aucun favori pour le moment.</p><p className="mt-2 text-sm text-slate-500">Ajoute tes cours et épreuves importants ici.</p></div>}</div></section></main>}
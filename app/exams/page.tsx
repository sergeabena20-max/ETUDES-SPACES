import Link from "next/link";
import { prisma } from "@/lib/prisma";

export const dynamic = "force-dynamic";

export default async function ExamsPage(){const exams=await prisma.exam.findMany({where:{status:"PUBLISHED"},include:{subject:true,academicLevel:true,solution:true},orderBy:{year:"desc"},take:24});return <main className="min-h-screen"><header className="border-b bg-white"><div className="container flex items-center justify-between py-4"><Link href="/" className="font-black">Études <span className="gradient-text">Space</span> 🇨🇲</Link><Link href="/dashboard" className="text-sm font-semibold text-sky-600">Mon espace</Link></div></header><section className="container py-10"><p className="text-sm font-semibold text-sky-600">📝 Entraînement</p><h1 className="mt-2 text-4xl font-black">Épreuves</h1><p className="mt-2 text-slate-500">Sujets, examens et corrections disponibles.</p><div className="mt-8 grid gap-4 md:grid-cols-2 lg:grid-cols-3">{exams.length?exams.map(e=><article key={e.id} className="card p-5"><div className="text-xs font-bold uppercase text-sky-600">{e.subject?.name||"Matière"} · {e.year||"—"}</div><h2 className="mt-3 text-xl font-bold">{e.title}</h2><p className="mt-2 text-sm text-slate-500">{e.description||"Épreuve disponible."}</p>{e.fileUrl&&<a className="mt-4 inline-block font-bold text-sky-600" href={e.fileUrl} target="_blank" rel="noreferrer">Télécharger →</a>}</article>):<div className="card p-8 md:col-span-3"><h2 className="font-bold">Les premières épreuves arrivent bientôt.</h2><p className="mt-2 text-sm text-slate-500">Les sujets seront ajoutés depuis l'administration.</p></div>}</div></section></main>}
import Link from "next/link";
import { redirect } from "next/navigation";
import { getCurrentUser } from "@/lib/session";

export default async function Dashboard(){const user=await getCurrentUser();if(!user)redirect("/login");return <main className="min-h-screen"><header className="border-b bg-white"><div className="container flex items-center justify-between py-4"><Link href="/" className="font-black">Études <span className="gradient-text">Space</span> 🇨🇲</Link><form action="/api/auth/logout" method="post"><button className="text-sm font-semibold text-slate-600">Déconnexion</button></form></div></header><section className="container py-10"><p className="text-sm font-semibold text-sky-600">Mon espace</p><h1 className="mt-2 text-4xl font-black">Bonjour {user.firstName} 👋🏾</h1><p className="mt-2 text-slate-500">Que veux-tu apprendre aujourd'hui ?</p><div className="mt-8 grid gap-4 sm:grid-cols-2 lg:grid-cols-4"><Link href="/courses" className="card p-6 hover:-translate-y-1 transition"><span className="text-3xl">📚</span><h2 className="mt-4 font-bold">Cours</h2><p className="mt-1 text-sm text-slate-500">Apprendre par matière.</p></Link><Link href="/exams" className="card p-6 hover:-translate-y-1 transition"><span className="text-3xl">📝</span><h2 className="mt-4 font-bold">Épreuves</h2><p className="mt-1 text-sm text-slate-500">S'entraîner avec des sujets.</p></Link><Link href="/favorites" className="card p-6 hover:-translate-y-1 transition"><span className="text-3xl">❤️</span><h2 className="mt-4 font-bold">Mes favoris</h2><p className="mt-1 text-sm text-slate-500">Retrouver mes contenus.</p></Link><Link href="/profile" className="card p-6 hover:-translate-y-1 transition"><span className="text-3xl">👤</span><h2 className="mt-4 font-bold">Mon profil</h2><p className="mt-1 text-sm text-slate-500">Mes informations.</p></Link>{(user.type === "ADMIN" || user.type === "SUPER_ADMIN") && <Link href="/admin" className="card p-6 hover:-translate-y-1 transition border-sky-200 bg-sky-50"><span className="text-3xl">⚙️</span><h2 className="mt-4 font-bold">Administration</h2><p className="mt-1 text-sm text-slate-500">Gérer la plateforme.</p></Link>}</div></section></main>}
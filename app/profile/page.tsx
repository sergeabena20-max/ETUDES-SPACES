import Link from "next/link";
import { redirect } from "next/navigation";
import { getCurrentUser } from "@/lib/session";

export default async function ProfilePage(){const user=await getCurrentUser();if(!user)redirect("/login");return <main className="min-h-screen"><header className="border-b bg-white"><div className="container flex items-center justify-between py-4"><Link href="/dashboard" className="font-black">Études <span className="gradient-text">Space</span> 🇨🇲</Link><Link href="/dashboard" className="text-sm text-sky-600">← Retour</Link></div></header><section className="container py-10"><h1 className="text-3xl font-black">Mon profil</h1><div className="card mt-6 max-w-2xl p-6"><dl className="grid gap-5 sm:grid-cols-2"><div><dt className="text-xs font-bold uppercase text-slate-400">Nom complet</dt><dd className="mt-1 font-semibold">{user.firstName} {user.lastName}</dd></div><div><dt className="text-xs font-bold uppercase text-slate-400">E-mail</dt><dd className="mt-1 font-semibold">{user.email}</dd></div><div><dt className="text-xs font-bold uppercase text-slate-400">Statut</dt><dd className="mt-1 font-semibold">{user.studentStatus==="ELEVE"?"Élève":user.studentStatus==="ETUDIANT"?"Étudiant":"—"}</dd></div><div><dt className="text-xs font-bold uppercase text-slate-400">Compte</dt><dd className="mt-1 font-semibold">{user.type}</dd></div></dl><p className="mt-6 text-sm text-slate-500">La modification complète du profil sera ajoutée dans la phase profils académiques.</p></div></section></main>}